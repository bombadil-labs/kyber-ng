# T14d — Boundary Enumeration: Failure-Mode Inventory

Status: OPEN (light ceremony — rumble for the design cells, ONE deterministic
build, no blind taste). Parent: T14c (`45a8eb9` on main, merged `4058707`).
Rails: carried (frozen `deps/ spec/ SPEC.md mix.exs config/`; no Process.sleep;
canonical spellings; content-derived identity; merge-is-union; retraction-is-negation).

## Scope

The systematic inventory of the policy layer's failure modes — every gate × input
interaction, every tool boundary, every engine path — pinned to an exact behavior
and witnessed by a deterministic test. The slice's deliverable is the MATRIX: a
cell per boundary case, each with a pinned outcome and a fails-when-absent leg.

In scope: the memory_policy clause table's undecidable cells (the T14c deferred
fork×undecodable + its neighbors), url_policy edge inputs, the tool boundary caps
(fs.read output, fs.list entries, web_fetch size/timeout, shell containment,
http.get redirects), the engine path pins (constructor fail-hard, replay
decode-failure raise — the T14c taste cells), the store/epoch mechanics
(fork-precision beyond two live heads, merge, retraction), and precision bounding
(exact values, exact behaviors — no "reasonable cap" language).

Out of scope: the M5 engine verdict bug (engine.ex:161 atom-vs-string guard —
the separate engine-decides slice; live config discipline still applies), URL
policy redesign, new tool surfaces, the operational/live ACs (all cells here are
deterministic), the channel adapter (Liet's import).

## The failure-mode inventory

### Class A — gate × input interactions (memory_policy, url_policy)

| Cell | Input state | Current behavior | Pin status |
|---|---|---|---|
| A1 | forked epoch × undecodable args | `:allow` (policy abstains; action validation owns it — T14c.md:157) | **OPEN (D1)** |
| A2 | forked epoch × valid args | refuse `memory_policy: epoch forked (fail closed)` | pinned (witness) |
| A3 | ungoverned × valid args | refuse `memory_policy: no governing epoch (fail closed)` | pinned (witness) |
| A4 | ungoverned × undecodable args | (clause-table gap) | **OPEN (D2)** |
| A5 | governed epoch × undecodable args | `:allow` (action validation owns it) | pin + witness |
| A6 | governed × entity allowed | `:allow` (check_memory) | pinned (witness) |
| A7 | governed × entity refused | refuse `memory_policy: entity not allowed by the current epoch` | pinned (witness) |
| A8 | epoch with zero allow_entities | refuse-all (unevolved epoch = vacuous) | pinned (witness) |
| A9 | url_policy: nil URL / malformed (no host) | TBD | **OPEN (D5)** |
| A10 | url_policy: empty allow_hosts / empty allow_schemes | TBD | **OPEN (D5)** |
| A11 | url_policy: scheme case / host case | downcase pin exists (constructor-normalized) | witness |

### Class B — tool boundaries

| Cell | Boundary | Pin status |
|---|---|---|
| B1 | fs.read output cap (bytes) + past-cap behavior (truncate-with-marker / error / refuse) | **OPEN (D6)** |
| B2 | fs.list entry cap + over-cap behavior | **OPEN (D7)** |
| B3 | shell containment rule + not-contained outcome | **OPEN (D8)** |
| B4 | web_fetch size cap + timeout bound + behavior | **OPEN (D9)** |
| B5 | http.get: `autoredirect: false` static witness (T14b L4) | pinned (witness) |
| B6 | memory.read unknown entity: canon nil ⇒ `{"", "unknown_entity"}` (T14c taste cell) | pinned (witness — the deepseek line has the leg; the matrix keeps it) |
| B7 | memory.read × forked epoch: canon resolution under fork | **OPEN (D3)** |
| B8 | memory.read wrong-typed / non-string entity arg: outcome (unknown_entity vs error vs refuse) | **OPEN (D10)** |

### Class C — engine paths

| Cell | Path | Pin status |
|---|---|---|
| C1 | `Events.prompt_assembled → {:error, _}`: fail-hard (match, never silent-skip) — T14c taste cell | **pin + witness (the deepseek line already matches; the matrix keeps it)** |
| C2 | replay pre-check decode failure on a stored claim: store corruption — raise, never re-assemble (T14c L2) | pinned (witness) |
| C3 | answered-skip idempotence: counted skip, zero new deltas | pinned (witness) |
| C4 | M5 verdict bug | OUT OF SCOPE (noted, separate slice) |

### Class D — store/epoch mechanics

| Cell | Mechanic | Pin status |
|---|---|---|
| D1 | merge-is-union on identical double-emission: one claim | pinned (witness) |
| D2 | retraction-is-negation: retracted epoch not current | pinned (witness) |
| D3 | fork detection precision: exactly two live heads ⇒ `{:error, :forked}`; THREE+ live heads ⇒ ? | **OPEN (D3)** |
| D4 | concurrency bound: in-flight cap + behavior at cap (queue / refuse / drop) | **OPEN (D4)** |

## Open decisions (the rumble's dispose list)

D1. A1 fork×undecodable: abstain-allow (status quo) / fail-closed refuse (forked
reason) / refuse with a dedicated reason string.
D2. A4 ungoverned×undecodable: abstain-allow / fail-closed refuse (ungoverned
reason).
D3. B7 + D3: memory.read under a forked epoch (refuse-before-resolve?) and the
>2-live-heads behavior (reuse `{:error, :forked}` / distinct error / treat as
forked regardless of count).
D4. D4 concurrency cap value + behavior at cap.
D5. A9/A10 url_policy nil/malformed/empty: refuse / error / allow.
D6. B1 fs.read cap value + past-cap behavior.
D7. B2 fs.list cap value + over-cap behavior.
D8. B3 shell containment rule + not-contained outcome.
D9. B4 web_fetch size/timeout values + behavior.
D10. B8 memory.read wrong-typed args outcome.

## Acceptance criteria

AC1 — Gate matrix: every Class A cell's outcome pinned and witnessed; the
clause-table cells (A2-A8) keep their T14c legs, the open cells (A1, A4, A9-A11)
get new legs. Fails-when-absent: deleting any clause-table row or matrix leg
fails the suite.
AC2 — Tool bounds: every Class B cap pinned to an exact value with an exact
behavior + a deterministic test at and past the cap. Fails-when-absent: removing
a cap or its leg fails the suite.
AC3 — Engine pins: C1 constructor-failure fails hard (a test injects the
`{:error, _}` path and asserts the hard failure, NOT a silent skip); C2/C3
witnessed. Fails-when-absent: re-introducing a silent skip passes nothing.
AC4 — Epoch mechanics: D1-D4 witnessed, including the >2-live-heads cell.
Fails-when-absent: weakening fork detection fails.
AC5 — Precision bounding: every cap value appears exactly once in the plan and
is referenced by its test (no duplicated literals, no "reasonable" language).

## Deliverables

- `test/kyber/agent/boundary_matrix_test.exs` (Class A matrix legs)
- `test/kyber/agent/tool_bounds_test.exs` (Class B caps + behaviors)
- `test/kyber/agent/engine_paths_test.exs` (Class C pins)
- `test/kyber/agent/epoch_mechanics_test.exs` (Class D witness legs)
- Bounded edits: `lib/kyber/agent/policy.ex` (clause table), 
  `lib/kyber/agent/tool_executor.ex` (bounds + B7/B8), 
  `lib/kyber/agent/engine.ex` (C1 fail-hard if not already exact)
- Spec §The failure-mode inventory completed (every OPEN cell resolved with the
  pinned outcome in the Design resolution)

## Completion gate

`mix test && test -f test/kyber/agent/boundary_matrix_test.exs && test -f
test/kyber/agent/tool_bounds_test.exs && test -f
test/kyber/agent/engine_paths_test.exs && test -f
test/kyber/agent/epoch_mechanics_test.exs`

## Design resolution (converged plan)

Synthesized from the T14d blind rumble: P1/P2/P3 → A1/A2/A3 → R1/R2/R3. The
dispositions converged (R2 switched its own D4/D6/D7 to the structural-cap /
licensed-home line; every P3 divergence on caps and rebuilds was
substrate-falsified). Reflect-round voice self-labels were unreliable (two
voices disposed their own pins as rivals'); the dispositions below are judged
by content and substrate verification, not slot. Every cell was verified
against `lib/` before landing here.

### Recorded spec defects (this spec's own table — corrected, not papered over)

- **S1 — C1's "the deepseek line already matches" is FALSE.** The merged
  `engine.ex` constructor-failure arm is `{:error, _reason} -> state` — the
  silent skip. The Class C table's C1 "pin + witness (already matches)" cell is
  corrected to: **bounded edit MANDATORY** (the raise, below).
- **S2 — A1's "Current behavior `:allow` (T14c.md:157)" is a mis-citation.**
  T14c's abstain ruling is `{:ok, epoch}`-gated; in the merged clause table the
  epoch arms (`:none`, `{:error, :forked}`) match before `extract_entity/1`
  (tool_executor.ex:226-250). The A1 cell's current-behavior column is
  corrected to: **refuse `memory_policy: epoch forked (fail closed)`** — A1/A4
  were enumeration gaps, never behavior gaps.
- **S3 — B4's `web_fetch` is a phantom.** No such tool exists in `lib/` (grep:
  only this spec names it). The B4 cell's surface is corrected to the existing
  **http.get / http.post** pair (Action.Http + the Httpc adapter). No new tool
  surface — recorded as a carry (below).

### The ten cells, resolved

**D1 — A1 fork × undecodable → REFUSE.** `{:refuse, "memory_policy",
"memory_policy: epoch forked (fail closed)", nil}` — the existing literal, no
new string; NO ToolResult. Epoch-arms-before-decode is NORMATIVE
(tool_executor.ex:226-250): args are never examined under fork. The spec's
A1 ":allow" column is recorded defect S2, corrected to refuse.

**D2 — A4 ungoverned × undecodable → REFUSE (memory) / recorded debt (url).**
Memory refuses `memory_policy: no governing epoch (fail closed)` (existing
literal), NO ToolResult — the `:none` row precedes args decode. The url twin
(`:none → :allow`) is pinned as behavior and recorded as **fail-open debt with
a deferred owner** (the governance-default slice) — witnessed as a tripwire so
the fix must consciously flip a test, never drift.

**D3 — B7 + n-head fork → refuse-before-resolve; any n≥2 ⇒ `{:error,
:forked}`.** Under a forked epoch, `memory.read` gets exactly one GateDecision
(forked reason, nil epoch), NO ToolResult, and `Memory.canon` NEVER runs —
refused known/unknown indistinguishable (no existence oracle). Fork detection
is count-insensitive: the `_two_heads` catch-all (policy.ex:140) is normative
for any n≥2 live heads — same tuple, same reason, no distinct error, no
winner-picking. Micro-fold: rename the clause comment `_two_or_more_heads`
(P3, comment-only).

**D4 — concurrency bound → cap 1 STRUCTURAL; at-cap QUEUE in commit order.**
The cap is the substrate's structure, verified: one GenServer `handle_cast`,
synchronous `LlmHandler.chat/3` inside the cast, zero Task/spawn in
`engine.ex`. NO new literal, NO new GateDecision surface. At/past cap:
requests queue in the mailbox, processed in commit order — never refuse, never
drop. Witness: blocking-stub leg (stub blocks on `receive :release` — sanctioned
synchronization), exactly one `{:llm_request, _}` pre-release. Recorded rider:
the mailbox is unbounded; the pin bounds WORK, and once-per-delta-id routing
bounds distinct work per delta to 1. **Rejected:** P2's 16-refuse (underived
literal — AC5's banned "reasonable cap"; engine-minted refusal crosses a layer
that only consumes `"decides"`) and P3's `@max_in_flight` (a literal with no
enforcement site — AC5 decoration).

**D5 — A9/A10 url degenerate inputs → converged outcomes.** (a) Non-binary /
absent `"url"` (JSON null, number, missing key, non-JSON args): policy ABSTAINS
→ the action answers (`"refused: url must be a string"`/`"refused"` for null;
missing-key `"error"`); no request. (b) Binary-but-malformed: refuse in
scheme-then-host order — no scheme ⇒ `url_policy: scheme not allowed by the
current epoch`; scheme ok + empty host ⇒ the host reason. (c) A10 vacuous
epoch: empty `allow_schemes` refuses all at the scheme clause; empty
`allow_hosts` at the host clause — no default exists, refuse-all stands.
Fixtures: `URI.parse("https://")` → host `""` (NEVER nil — substrate-verified;
P3's nil≡empty collapse is moot); both proposed fixture sets land the same
reason.

**D6 — B1 fs.read cap → 65_536 via `context[:output_cap]` (action.ex:32), in
an executor fs.read-keyed clause.** No new value — the existing
`@default_output_cap` home; tests inject 1024 (action_bounds idiom). Past-cap:
`binary_part(content, 0, cap) <> "\n" <> Action.truncation_marker(cap)`,
status `"ok"`; at exactly cap, untruncated, no marker. The `"\n"` joiner IS
the house idiom (http.ex:97, shell.ex:171) — P3's joiner-less math is
off-by-one. The executor clause (not an `Action.Fs` edit) is the licensed home;
a uniform executor cap would re-truncate self-capped actions' marker tails.

**D7 — B2 fs.list cap → 1024, home in the ToolExecutor bounds clause.** The
one NEW literal, one home, beside its marker: `[truncated: listing exceeded
the 1024-entry cap]` (count-worded, new spelling). Over-cap: first 1024 of the
sorted listing + marker line, status `"ok"`; the sort PRE-EXISTS (fs.ex:71) —
no determinism pin needed. P3's 1_000 rejected (forks the only free literal
for no stated reason; house caps are powers of two).

**D8 — B3 shell containment → the P1/P2 shared statement.** The contract is
EXACTLY four bounds: (1) cwd sandboxed to the workspace root; (2) env scrubbed
to exactly PATH/HOME/TMPDIR (shell.ex:179-192); (3) 5_000 ms hard tree-kill;
(4) output cap with marker. **fs-escape RUNS as the declared hole** — lexical
command screening is a placebo and is never the boundary; GOVERNANCE (the
permission gate) is the control. Witnessed as the declared-hole leg (escaped
read returns `"ok"` WITH content; env output = exactly the three variables) so
later closure is a visible diff. The fs refusal spelling is the EXISTING
`"refused: " <> path <> " escapes the workspace root"` (fs.ex:177-179) —
P3's rebuild (status `"error"`, new spelling, symlink-as-debt) is FALSIFIED:
the two-layer resolve (lexical + symlink-real) already exists at fs.ex:93-119,
and fs.write already routes through it (fs.ex:46). Folds: P2's shell.ex
moduledoc containment statement + B3b/B3c static default-value witnesses.

**D9 — B4 → surface corrected to http.get/http.post (S3 phantom).** Existing
bounds stand, no new value: 65_536 body cap (http.ex:39), truncate-with-marker
`"ok"`; POST request body over-cap REFUSED, never truncated; adapter timeouts
GET `30_000` / POST `120_000` / connect `10_000` (http_client.ex:44-45,
98-99), pinned by a static option-list witness (the only deterministic form —
no Process.sleep). Timeout behavior: `{:error, reason}` maps via
`inspect(reason)` to `"http.get <url>: :timeout"` / status `"error"`
(http.ex:53 — P2's spelling, P1's concession folded). P3's
262_144 / 10_000 / `"timeout"`-status / new-marker line is FALSIFIED against
all three homes.

**D10 — B8 wrong-typed entity → action-validation error.** Policy abstains;
the run clause answers `"malformed action arguments: " <> args` / `"error"`
(tool_executor.ex:335). NEVER `unknown_entity` (reserved for a well-formed
string id with canon nil — B6), NEVER refuse (a refusal would re-open the
existence-oracle question). The `""` sub-cell settles by epoch seed: the gate
refuses `memory_policy: entity not allowed by the current epoch` unless `""`
is allow-listed, in which case B6's `{"", "unknown_entity"}` holds (P3's A14
guard folded).

### Class C — the engine pins

**C1 — the bounded edit is MANDATORY (defect S1).** The `{:error, reason}`
arm becomes `raise "PromptAssembled construction failed: " <> inspect(reason)`
(the P2/P3 spelling — names the constructor; the siblings are the "store
corruption" raises). The raise stays mandatory even though the tuple path is
engine-unreachable today: corrupt-seed discriminators are MECHANICALLY
IMPOSSIBLE — `Keys.author_for_seed/1` → `seed_bytes!/1` raises at events.ex:256
while building the claims map, BEFORE the `{:error, _}` arm; any corrupt-seed
leg passes with the silent skip restored (an inverted discriminator). The
suite-visible witness is P1's two-tier form: (a) the constructor-contract leg
(`Events.prompt_assembled(seed, "not-a-number", …)` ⇒
`{:error, :timestamp_not_a_number}`); (b) the engine-posture DOWN leg. The
residual (no store-state witness discriminates raise from skip) is RECORDED,
enforced at the diff-metric/review layer — the premortem owns attacking it.

**C2 — kept + legged** (both pre-check arms: `"decode failed"` / `"no content
pointer"`; exactly-one PromptAssembled preserved). **C3 — kept** (counted
skip, delta count unchanged, zero new model calls).

### Folds absorbed (the UNARGUEDs + argue-round merges — all cited, none conflict)

- **C4b** (P2): post-emit decode-fail raise leg (`"canonical does not
  decode"`) — the third corruption arm the C2 legs miss.
- **A12** (P2): entity-id case sensitivity — NO downcase on entity ids
  (host-downcase is T14b host-specific); `allow_entities ["Alice"]` refuses
  `"alice"`. The contrast row for A11.
- **B10** (P3): GateDecision replay precedence — a stored refusal re-emits
  verbatim even after the epoch evolves to allow; memory-family leg.
- **B11** (P3): fs.write containment witness — past-root write refused, target
  file provably not created (behavior exists via the shared resolve; the LEG is
  the fold).
- **A13-tripwire wording** (P3): the url ungoverned fail-open hole witnessed
  as a tripwire — the deferred slice must consciously flip a test, never drift.
- **A11b** (P1): scheme-case asymmetry — epoch `allow_scheme` values are NOT
  constructor-downcased; a stored `"HTTPS"` refuses all (fail-closed
  asymmetry, recorded).
- **B2 newline edge** (P1): a filename containing `"\n"` breaks the fs.list
  line grammar (counts as two entries) — recorded, not handled.
- **B3b/B3c** (P2): static default-value witnesses for `@default_shell_timeout`
  5_000 and `@default_output_cap` 65_536 at their homes (not just injected
  values).
- **`_two_or_more_heads`** (P3): comment-only rename in policy.ex.

### Carries (recorded debt, deferred owners)

1. **url_policy ungoverned `:none → :allow`** — fail-open SSRF-shaped debt;
   owner: the deferred governance-default slice; tripwire leg in this slice.
2. **The phantom `web_fetch`** — the spec named a tool that does not exist;
   any future fetch surface is a NEW slice, not a B4 amendment.
3. **C1 witness residual** — the constructor `{:error, _}` tuple is
   engine-unreachable (S3-verified); the raise-vs-skip discrimination lives at
   the review/diff layer until the constructor grows a reachable error path.

### The inventory, completed

| Cell | Resolution |
|---|---|
| A1 | refuse forked (D1; spec column corrected — defect S2) |
| A4 | refuse ungoverned (D2) |
| A9/A10 | abstain-on-non-binary → action; scheme-then-host refusals; vacuous refuse-all (D5) |
| A11b/A12/A13 | folds — scheme-case asymmetry / entity-case / url tripwire |
| B1 | 65_536 via `:output_cap`, executor fs.read clause, `"\n"`+marker, "ok" (D6) |
| B2 | 1024, ToolExecutor home, count-worded marker, sorted prefix (D7) + newline edge recorded |
| B3 | four-bounds statement; escape runs as declared hole; existing fs refusal spelling (D8) + B3b/B3c |
| B4 | http.get/post surface; 65_536 / 30_000 / 120_000 / 10_000 kept; timeout → "error" via `inspect(reason)` (D9) |
| B7 | refuse-before-resolve, canon never runs (D3) |
| B8 | malformed-args "error"; `""` by epoch seed (D10) |
| B10/B11 | folds — replay precedence / write containment |
| C1 | MANDATORY raise `"PromptAssembled construction failed: " <> inspect(reason)`; two-tier witness; residual recorded (defect S1) |
| C2/C3/C4b | kept + legged (C4b fold) |
| D3 (class D) | any n≥2 live heads ⇒ `{:error, :forked}` (D3) |
| D4 (class D) | cap 1 structural, queue in commit order (D4) |

Licensed edit homes: `tool_executor.ex` (fs.read cap clause, fs.list cap +
marker, the one new literal), `engine.ex` (the C1 raise), `policy.ex`
(comment-only rename). No `action/*.ex` code edits except the shell.ex
moduledoc statement (P2 fold — doc-only). Zero new reason strings; two new
spellings total (the fs.list marker, the C1 raise).

### Diff metric

The build matches the resolution on exactly these element classes: cap VALUES
(65_536 / 1024 / 5_000 / 30_000 / 120_000 / 10_000 / structural-1), reason
strings (all existing Policy literals, zero new), clause-table cells
(epoch-arms-before-decode, scheme-then-host, refuse-before-resolve),
marker/raise spellings (`[truncated: listing exceeded the 1024-entry cap]`,
`"PromptAssembled construction failed: " <> inspect(reason)`, the `"\n"`
joiner), and licensed edit homes (tool_executor.ex / engine.ex / policy.ex
comment) — any deviation in any class is a diff-metric miss.

Difficulty per contested decision: D4 **hard** (structural-vs-literal), D5
**easy** (fixtures only), D6 **subtle** (joiner off-by-one), D7 **easy**
(free literal), D8 **moderate** (substrate falsification), D9 **easy**
(falsified outright), C1 **treacherous** (inverted discriminators).
