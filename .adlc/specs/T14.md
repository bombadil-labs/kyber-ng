# T14 — The Reactor: the agent runs (draft, staged from the premortem carries)

Status: DRAFT — staged from the T12 + T13 premortem carries + build findings. Not yet
rumbled (the P1 premortem runs on THIS spec BEFORE any build leg — the corrected order).

## The slice

The agent runs: an event-driven reactor that closes the loop the T12/T13 slices left open —
refusals come back to the model, prompts are answered as store deltas, policy can evolve
without restart, and the boundary is stated instead of implied.

## Deliverables (draft)

- `lib/kyber/agent/reactor.ex` — the event-driven loop (T14 title): fires on deltas, budgets
  per turn, gated initiation (the Oracle seed).
- The refusal loop: a gate-refused ToolCall resumes the model's turn with the reason
  (T12 carry #1 — currently the refusal is attested but the turn hangs forever).
- Prompt-answer-as-delta: the `prompt` policy's answer is an attested store delta; the
  decision derives from store state (determinism clause preserved); no-answer-within-T →
  refuse; the answering key is restricted to human authority (T12 carry #2).
- Idempotence enforcement: idempotence as a registry field; non-idempotent actions refuse
  allow-listing without explicit override; AC1 sub-assertion for the pre-persistence re-fire
  window (T12 carry #3).
- URL policy for http actions: scheme/host allow-list or loopback/link-local deny-list,
  resolved at boot like the gate; redirect targets re-validated (T12 carry #4).
- Policy epochs: gate policy reload without restart; revocation is retroactive — a stored
  allow decision does NOT outrank a later revocation (T12 carry #5).
- Memory-read permission seam: the retriever is gated like actions; a refused memory read
  degrades to a precision-only turn, never a dead turn (T13 carry).

## Carries to be folded into this spec at rumble time (full list)

T12 premortem: gate-refusal-hang, prompt-policy determinism hole, idempotence
documented-not-enforced, bounded-by-construction overclaim (shell not-contained statement,
fs.read output cap, concurrency bound), HTTP SSRF-open, boot-frozen policy + non-retroactive
revocation, A/B seam contract, attestation keys, AC3 bound enumeration (fs.list, over-cap
request behavior).

T13 premortem: AC4 placebo (FIXED pre-merge; channel-union assertion — VERIFIED LIVE 2026-08-07: real kimi-k3 run answered 'The relay deploy is pinned to commit 9f2ac4.', recording at /tmp/kyber-t13-ac4-record-272.jsonl), below-prompt heuristic (FIXED
pre-merge), O(store) ×2 per turn (projection candidate), df-saturated decay (pinned, steady
state), cold-start (pinned property), memory-read seam, one-N-one-split under :window
override (FIXED pre-merge), divergent_cap ceiling (FIXED pre-merge), restart determinism
(FIXED pre-merge), normalize/1 refusal (FIXED pre-merge), human_author leg parity (FIXED
pre-merge).

T13 build findings: cite-fan cap (FIXED pre-merge), empty pointer tail (by construction;
precision bounding is a T14 candidate).

T11c carry: operator-key boot wiring for signer-derived provenance (the reason-string
fallback is the documented stopgap).

## Process commitments (the corrected ceremony)

- P1 premortem runs on THIS spec BEFORE the build campaign; its findings go into the fork
  briefs.
- Merge to main happens only after P5 + fixes — the fix-review loop is the merge gate.
